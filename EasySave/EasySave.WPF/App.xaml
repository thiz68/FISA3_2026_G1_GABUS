<Application x:Class="EasySave.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EasySave.WPF"
             xmlns:converters="clr-namespace:EasySave.WPF.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- 1. Active theme (Light by default — ThemeManager swaps this at index 0) -->
                <ResourceDictionary Source="Themes/Light.xaml"/>
                <!-- 2. Global control styles (reference theme brushes via DynamicResource) -->
                <ResourceDictionary Source="Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters (application-scoped, referenced by all views) -->
            <converters:BackupTypeConverter x:Key="BackupTypeConverter"/>
            <BooleanToVisibilityConverter  x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
