<UserControl x:Class="EasySave.WPF.Views.JobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding BackupJobsTitle, FallbackValue=Jobs}" Style="{StaticResource Text.Title}"/>
            <TextBlock Text="Create, edit and execute backup jobs." Style="{StaticResource Text.Subtitle}" Margin="0,6,0,0"/>
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,2,0,0">
            <Button Content="{Binding AddJobText}"
                    Margin="6,0,0,0"
                    Style="{StaticResource Button.Primary}"
                    Command="{Binding AddJobCommand}"/>

            <Button Content="{Binding ExecuteAllText}"
                    Margin="10,0,0,0"
                    Command="{Binding ExecuteAllCommand}"/>

            <Button Content="{Binding ExecuteSelectedText}"
                    Margin="10,0,0,0"
                    Command="{Binding ExecuteSelectedCommand}"/>
        </StackPanel>

        <!-- DataGrid -->
        <Border Grid.Row="2"
                Background="{DynamicResource Brush.Surface}"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="0">

            <DataGrid ItemsSource="{Binding Jobs}"
                      SelectedItem="{Binding SelectedJob}"
                      AutoGenerateColumns="False">

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="52">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="{Binding DataContext.NameHeader, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Binding="{Binding Name}" Width="*" />

                    <DataGridTextColumn Header="{Binding DataContext.SourceHeader, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Binding="{Binding SourcePath}" Width="2*" />

                    <DataGridTextColumn Header="{Binding DataContext.TargetHeader, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Binding="{Binding TargetPath}" Width="2*" />

                    <DataGridTextColumn Header="{Binding DataContext.TypeHeader, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Binding="{Binding TypeDisplay}" Width="*" />

                    <DataGridTemplateColumn Header="{Binding DataContext.ActionsHeader, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="{Binding DataContext.EditText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Margin="0,0,8,0"
                                            Command="{Binding DataContext.EditJobCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>

                                    <Button Content="{Binding DataContext.DeleteText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Style="{StaticResource Button.Danger}"
                                            Command="{Binding DataContext.DeleteJobCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

            </DataGrid>
        </Border>

        <!-- Dialog Overlay -->
        <Grid Grid.RowSpan="3"
              Visibility="{Binding IsDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}">

            <Border Background="#99000000"/>

            <Border Width="560"
                    Background="{DynamicResource Brush.Surface}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="18"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                <StackPanel>
                    <TextBlock Text="{Binding AddJobText}" Style="{StaticResource Text.Section}" Margin="0,0,0,14"/>

                    <TextBlock Text="{Binding NameHeader}" Style="{StaticResource Text.Subtitle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding DialogJobName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

                    <TextBlock Text="{Binding SourceHeader}" Style="{StaticResource Text.Subtitle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding DialogSourcePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

                    <TextBlock Text="{Binding TargetHeader}" Style="{StaticResource Text.Subtitle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding DialogTargetPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

                    <TextBlock Text="{Binding TypeHeader}" Style="{StaticResource Text.Subtitle}" Margin="0,0,0,6"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <RadioButton Content="Full"
                                     IsChecked="{Binding DialogType, Converter={StaticResource BackupTypeConverter}, ConverterParameter=full}"
                                     Margin="0,0,18,0"/>
                        <RadioButton Content="Differential"
                                     IsChecked="{Binding DialogType, Converter={StaticResource BackupTypeConverter}, ConverterParameter=diff}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel"
                                Margin="0,0,10,0"
                                Command="{Binding CancelDialogCommand}"/>
                        <Button Content="Save"
                                Style="{StaticResource Button.Primary}"
                                Command="{Binding SaveJobCommand}"/>
                    </StackPanel>
                </StackPanel>

            </Border>
        </Grid>
    </Grid>
</UserControl>
