<UserControl x:Class="EasySave.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding DashboardTitle}" Style="{StaticResource Text.Title}"/>
            <TextBlock Text="{Binding StateFileTitle}" Style="{StaticResource Text.Subtitle}" Margin="0,6,0,0"/>
        </StackPanel>

        <!-- State -->
        <Border Grid.Row="2"
                Background="{DynamicResource Brush.Surface}"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="14">
            <StackPanel>
                <TextBlock Text="{Binding StateFileTitle}" Style="{StaticResource Text.Section}" Margin="0,0,0,10"/>
                <TextBox Height="220"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Text="{Binding StateContent, Mode=OneWay}"
                         IsReadOnly="True"
                         FontFamily="{StaticResource Font.Mono}"
                         FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- Log -->
        <Border Grid.Row="4"
                Background="{DynamicResource Brush.Surface}"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="14">
            <StackPanel>
                <TextBlock Text="{Binding LogFileTitle}" Style="{StaticResource Text.Section}" Margin="0,0,0,10"/>
                <TextBox Height="220"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Text="{Binding LogContent, Mode=OneWay}"
                         IsReadOnly="True"
                         FontFamily="{StaticResource Font.Mono}"
                         FontSize="12"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
