<Window x:Class="EasySave.WPF.Views.BackupProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{Binding TitleText}"
        Height="440" Width="740"
        MinHeight="380" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Separator -->
            <RowDefinition Height="16"/>
            <!-- DataGrid card -->
            <RowDefinition Height="*"/>
            <!-- Footer gap -->
            <RowDefinition Height="16"/>
            <!-- OK button -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Header ─────────────────────────────────────── -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="&#xE896;"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="20"
                       Foreground="{DynamicResource AccentBrush}"
                       VerticalAlignment="Center"
                       Margin="0,0,12,0"/>
            <TextBlock Text="{Binding HeaderText}"
                       FontFamily="Segoe UI Variable Display, Segoe UI"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- ── Progress DataGrid Card ─────────────────────── -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="12"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2"
                                  Direction="270" Color="#28000000"
                                  Opacity="1"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding JobProgressItems}"
                      IsReadOnly="True"
                      SelectionMode="Single">

                <DataGrid.Columns>

                    <!-- Job Name -->
                    <DataGridTextColumn
                        Header="{Binding DataContext.NameColumnHeader,
                                 RelativeSource={RelativeSource AncestorType=Window}}"
                        Binding="{Binding JobName}"
                        Width="2*"/>

                    <!-- Progress -->
                    <DataGridTemplateColumn
                        Header="{Binding DataContext.ProgressColumnHeader,
                                 RelativeSource={RelativeSource AncestorType=Window}}"
                        Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ProgressDisplay}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontWeight="Medium">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource PrimaryTextBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFailed}" Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource DangerTextBrush}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Actions: Pause/Resume + Emergency Stop -->
                    <DataGridTemplateColumn
                        Header="{Binding DataContext.ActionColumnHeader,
                                 RelativeSource={RelativeSource AncestorType=Window}}"
                        Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,4">

                                    <!-- Pause / Resume -->
                                    <Button Content="{Binding PauseResumeButtonText}"
                                            MinWidth="88"
                                            Height="30"
                                            Margin="0,0,8,0"
                                            Style="{StaticResource PauseButtonStyle}"
                                            Command="{Binding PauseResumeCommand}"/>

                                    <!-- Emergency Stop -->
                                    <Button Content="{Binding DataContext.EmergencyStopText,
                                                      RelativeSource={RelativeSource AncestorType=Window}}"
                                            MinWidth="88"
                                            Height="30"
                                            Style="{StaticResource DangerButtonStyle}"
                                            Command="{Binding EmergencyStopCommand}"/>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ── OK button (shown when complete) ───────────── -->
        <Button Grid.Row="4"
                Content="{Binding OkText}"
                MinWidth="120"
                Height="40"
                HorizontalAlignment="Center"
                Style="{StaticResource ActionButtonStyle}"
                Command="{Binding CloseCommand}"
                Visibility="{Binding IsCompleted,
                             Converter={StaticResource BooleanToVisibilityConverter}}"/>

    </Grid>
</Window>
