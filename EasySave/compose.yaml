services:
  logserver:
    build: .
    container_name: easysave_logserver
    ports:
      - "5000:5000"
    volumes:
      - log_data:/app/CentralLogs
    restart: unless-stopped

volumes:
  log_data: