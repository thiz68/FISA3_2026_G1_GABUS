<UserControl x:Class="EasySave.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <StackPanel MaxWidth="700">

            <TextBlock Text="{Binding GeneralSettingsTitle}" Style="{StaticResource Text.Title}" Margin="0,0,0,18"/>
            <TextBlock Text="Configuration used by the app." Style="{StaticResource Text.Subtitle}" Margin="0,0,0,18"/>

            <Border Background="{DynamicResource Brush.Surface}"
                    BorderBrush="{DynamicResource Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="16">
                <StackPanel>

                    <TextBlock Text="{Binding LogFormatLabel}" Style="{StaticResource Text.Section}" Margin="0,0,0,8"/>
                    <ComboBox ItemsSource="{Binding LogFormats}"
                              SelectedIndex="{Binding SelectedLogFormatIndex}"
                              Margin="0,0,0,16"/>

                    <TextBlock Text="{Binding ExtensionsToEncryptLabel}" Style="{StaticResource Text.Section}" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding ExtensionsToEncrypt, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,16"/>

                    <TextBlock Text="{Binding BusinessSoftwareLabel}" Style="{StaticResource Text.Section}" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding BusinessSoftware, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,18"/>

                    <Button Content="{Binding SaveSettingsText}"
                            Width="170"
                            HorizontalAlignment="Left"
                            Style="{StaticResource Button.Primary}"
                            Command="{Binding SaveSettingsCommand}"/>

                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
